DECLARE @sql NVARCHAR(MAX) = N'';

SELECT @sql += 'ALTER TABLE [' + OBJECT_NAME(parent_object_id) + '] DROP CONSTRAINT [' + name + '];' + CHAR(13)
FROM sys.foreign_keys
WHERE referenced_object_id = OBJECT_ID('userMemberships');

EXEC sp_executesql @sql;

DROP TABLE userMemberships;

--------------------

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE TABLE [dbo].[userMemberships](
	[membershipId] [int] IDENTITY(1,1) NOT NULL,
	[userId] [int] NOT NULL,
	[planId] [int] NOT NULL,
	[startDate] [datetime] NOT NULL,
	[endDate] [datetime] NOT NULL,
	[isActive] [bit] NOT NULL default (1), 
	[membershipStatus] CHAR(1) NOT NULL DEFAULT 'P' CHECK ([membershipStatus] IN ('P', 'A', 'R', 'E')),	
	[createdAt] [datetime] NOT NULL
) ON [PRIMARY]
GO
ALTER TABLE [dbo].[userMemberships] ADD PRIMARY KEY CLUSTERED 
(
	[membershipId] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, SORT_IN_TEMPDB = OFF, IGNORE_DUP_KEY = OFF, ONLINE = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
GO
ALTER TABLE [dbo].[userMemberships] ADD  DEFAULT (getdate()) FOR [createdAt]
GO
ALTER TABLE [dbo].[userMemberships]  WITH CHECK ADD FOREIGN KEY([planId])
REFERENCES [dbo].[plans] ([planId])
GO
ALTER TABLE [dbo].[userMemberships]  WITH CHECK ADD FOREIGN KEY([userId])
REFERENCES [dbo].[users] ([userId])
GO
